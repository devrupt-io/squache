# Upstream Proxy Configuration
# This file is managed by the Squache backend API
# Do not edit manually - changes will be overwritten

# Default: direct connection (no upstream proxy)
# Upstream proxies are configured via the API
